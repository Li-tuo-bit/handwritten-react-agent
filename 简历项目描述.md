# 简历项目描述 —— QuantAgent 多 Agent 量化研究系统

## 版本一：精简版（适合一页简历）

### 多 Agent 量化研究系统 | 个人项目

**技术栈**：Python · LangGraph · LangChain · Backtrader · DeepSeek API · ChromaDB · Pandas

- 设计并实现了基于 LangGraph 的 6-Agent 协作架构（Orchestrator + Data/Tech/RAG/Strategy/Backtest），支持自然语言驱动的一站式量化分析链路
- 实现 **StrategyAgent**：基于 Few-shot Prompt 调用 DeepSeek API 动态生成可执行的 Backtrader 策略代码，自动处理 markdown 残留、非法类名等 LLM 输出异常
- 实现 **BacktestAgent**：通过 `exec()` 动态加载策略类，集成 Cerebro 回测引擎，自动输出收益率、夏普比率、最大回撤等核心指标
- 完成 3 个端到端测试案例（综合研究 / 策略回测 / 技术面分析），系统链路零错误通过
- 项目地址：github.com/Li-tuo-bit/handwritten-react-agent

---

## 版本二：详细版（适合技术岗/量化岗）

### QuantAgent —— 多 Agent 量化研究系统 | 独立开发

**项目概述**
基于 LangGraph 构建的多 Agent 协作量化分析平台，用户通过自然语言输入即可自动完成数据获取、技术分析、研报检索、策略生成与回测验证的全流程。

**核心模块**

| 模块 | 技术方案 | 成果 |
|------|----------|------|
| **Orchestrator** | 关键词意图识别 + 条件路由（LangGraph Conditional Edges） | 自动编排 Agent 执行顺序，处理依赖关系（回测依赖策略，策略依赖数据） |
| **DataAgent** | AKShare 数据接口 + 自定义指标计算（MA/RSI/MACD） | 自动获取 A 股 K 线数据并计算技术指标，输出标准化 CSV |
| **TechAgent** | Pandas 数据切片 + 多指标信号融合 | 输出 MA 金叉/死叉、RSI 超买超卖、MACD 柱转向等交易信号 |
| **RAGAgent** | ChromaDB 向量数据库 + BGE 中文嵌入模型 | 检索研报并生成投资摘要，支持语义检索 |
| **StrategyAgent** | DeepSeek API + Few-shot Prompt | LLM 动态生成可执行 Python 策略类，支持双均线/MACD/RSI 等策略类型 |
| **BacktestAgent** | `exec()` 动态加载 + Backtrader Cerebro | 自动执行回测，输出总收益率、夏普比率、最大回撤 |

**技术难点与解决方案**

- **LLM 生成代码的可靠性**：Prompt 设计 + 后置清理（去除 markdown 标记、修复非法类名、验证 `bt.Strategy` 继承关系），确保生成代码 100% 可执行
- **动态策略加载**：通过 `exec()` 在隔离 namespace 中执行策略代码，配合 `issubclass()` 兜底查找，解决类名不固定问题
- **多 Agent 状态管理**：基于 LangGraph 的 `StateGraph` 实现全局状态共享，每个 Agent 节点遵循统一的状态更新范式（`plan_step` 递增 + `current_agent` 标记）

**测试验证**

- 案例 A（综合研究）：`rag_agent → data_agent → tech_agent` → 输出研报摘要 + 技术面分析
- 案例 B（策略回测）：`data_agent → strategy_agent → backtest_agent` → 自动生成策略 + 回测报告
- 案例 C（技术面分析）：`data_agent → tech_agent` → 输出多指标交易信号

**项目收获**

- 深入理解了 LangGraph 的状态机模型和条件路由机制
- 掌握了 LLM 生成代码的 Prompt 工程技巧（Few-shot、约束条件、兜底处理）
- 实践了量化回测框架 Backtrader 的 Cerebro 引擎和 Analyzers 体系

---

## 版本三：一句话版本（适合放在自我评价或项目列表）

> 独立开发基于 LangGraph 的多 Agent 量化系统，实现 LLM 自动生成可执行交易策略并自动回测验证，3 个端到端案例稳定通过。

---

## 版本四：面试口述版（2 分钟介绍）

"这是我独立做的一个多 Agent 量化研究系统，核心思路是用 LangGraph 编排多个 Agent 协作完成从数据到策略到回测的全流程。

用户说一句'用双均线策略回测宁德时代'，系统会自动：
1. Orchestrator 识别意图，编排执行计划
2. DataAgent 获取股票数据
3. StrategyAgent 调用 DeepSeek API 生成 Backtrader 策略代码
4. BacktestAgent 动态加载策略，用 Cerebro 执行回测
5. 输出收益率、夏普比率、最大回撤

整个系统通过 LangGraph 的状态机管理，每个 Agent 节点共享全局 State，实现了自然语言驱动的一站式量化分析。"

---

**使用建议**：
- 投**量化/金融科技岗**：用版本二（突出 Backtrader、回测指标、策略生成）
- 投**AI/大模型应用岗**：用版本一 + 强调 LangGraph + LLM Prompt 工程
- 投**后端/Python 岗**：强调架构设计、异常处理、模块化设计
